@model IEnumerable<mvcwebsitesi.Models.ModelKategori>

    <div class="widget">
        <h4>KATEGORİLER</h4>
        <ul class="list-unstyled link-list">

            @foreach (var item in Model)
            {
                <li><i class="fa fa-angle-right fa-fw" aria-hidden="true"></i> <a href="/CiceklerUrun/List/@item.Id">@item.Ad <span class="pull-right">(@item.Adet)</span></a></li>

            }
        </ul>
    </div>
